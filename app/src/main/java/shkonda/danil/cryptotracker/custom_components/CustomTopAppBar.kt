package shkonda.danil.cryptotracker.custom_components

/*

@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun CustomTopAppBar(
    title: String
) {
    TopAppBar(
        */
/*title = {
            Column {
                Text(text = title, fontSize = 20.sp, modifier = Modifier.padding(bottom = 8.dp))
                Row(horizontalArrangement = Arrangement.SpaceBetween) {
                    SuggestionChip(onClick = { selectedCurrency = "usd" }, label = { Text("USD") })
                    SuggestionChip(onClick = { selectedCurrency = "rub" }, label = { Text("RUB") })

                }
            }
        }*//*

    )
}*/
